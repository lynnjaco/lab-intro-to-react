import "./NavBar.css"

export const NavBar = () => {
    return (
        <div id="header-nav-menu">
            <a href="#">Dashboard</a>
            <a href="#">Profile View</a>
            <a href="#">Profile Settings</a>
            <a href="#">Help</a>
        </div>
    )
}